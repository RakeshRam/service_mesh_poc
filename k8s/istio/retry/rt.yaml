apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: services-retry
spec:
  hosts:
  - mydemo.info
  http:
  - route:
    - destination:
        host: services-app-service
    retries:
      attempts: 10
      perTryTimeout: 2s